<% items ||= [] %>
<% items.each do |item| %>
  <% if !item.nil? && item.errors.any? %>
      <div class="alert alert-danger">
        <ul>
          <% item.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
  <% end %>
<% end %>